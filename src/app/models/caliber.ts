export class Caliber {
  caliberId: number = 0;
  caliberName: string = '';

  constructor(caliberId: number, caliberName: string) {
    this.caliberId = caliberId;
    this.caliberName = caliberName;
  }
}
